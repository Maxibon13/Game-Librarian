# Dependencies / build output
node_modules
dist
build
release
# Embedded Python runtime fetched by installer/build_release.ps1 (shipped as resources/python)
runtime

# Local-only: installer & launcher sources (only installer/*.exe ships)
installer/src

# Local-only: experiments, scratch scripts, notes
dev

# Runtime junk
run_log.txt
__pycache__/
*.pyc
